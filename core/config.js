/**
 * Bilim Arena — подключение к Supabase.
 *
 * Пусто — платформа работает как раньше: всё хранится на устройстве.
 * Заполнено — появляются аккаунты, классы по коду и общие данные.
 *
 * Сюда вставляются ТОЛЬКО Project URL и публичный anon-ключ
 * (Supabase → Project Settings → API). Они и должны быть видны в браузере:
 * доступ к данным защищают правила RLS из supabase/schema.sql.
 * Ключ service_role сюда вставлять НЕЛЬЗЯ — он обходит все правила.
 */
export const SUPABASE = {
  url: '',
  anonKey: ''
};
